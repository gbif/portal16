<div class="container--desktop m-t-1" ng-if="speciesKey2.capabilities && speciesKey2.capabilities.total > 0">
    <h4 class="text-muted small text-uppercase">{{ speciesKey2.capabilities.total | localNumber }}  georeferenced records</h4>
    <div class="card">
        <div ng-if="speciesKey2.mapFilter">
            <map-widget filter="speciesKey2.mapFilter"></map-widget>
        </div>
    </div>
</div>