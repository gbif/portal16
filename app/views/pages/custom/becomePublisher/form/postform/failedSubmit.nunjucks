<section id="step_failed" ng-if="eoi.state.submissionComplete && eoi.state.submissionError">
    <h2>Submission failed</h2>
    <p>For some reason, your submission failed.</p>
    <p>Please contact us about your interest at helpdesk@gbif.org.</p>
</section>