<section class="horizontal-stripe--paddingless">
    <div class="container--normal">
        <div ng-if="grbioPersonKey.person.mailingAddress.city">
            <h4 class="card-header--bold m-t-1" translate="grbio.contact.mailingAddress">Mailing address</h4>
            <div class="card card--spaced">
                <div class="card__content">
                    <dl class="inline container--fluid">
                        <div class="row" ng-if="grbioPersonKey.person.email">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.email"></dt>
                            <dd class="col-md-8 col-xs-12"><a ng-href="mailto:'{{grbioPersonKey.person.email}}'">{{grbioPersonKey.person.email}}</a></dd>
                        </div>
                        <div class="row" ng-if="grbioPersonKey.person.phone">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.phone"></dt>
                            <dd class="col-md-8 col-xs-12">{{grbioPersonKey.person.phone}}</dd>
                        </div>
                        <div class="row" ng-if="grbioPersonKey.person.fax">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.fax"></dt>
                            <dd class="col-md-8 col-xs-12">{{grbioPersonKey.person.fax}}</dd>
                        </div>
                        <div class="row" ng-if="grbioPersonKey.person.mailingAddress.address">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.address"></dt>
                            <dd class="col-md-8 col-xs-12">{{grbioPersonKey.person.mailingAddress.address}}</dd>
                        </div>
                        <div class="row" ng-if="grbioPersonKey.person.mailingAddress.province">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.province"></dt>
                            <dd class="col-md-8 col-xs-12">{{grbioPersonKey.person.mailingAddress.province}}</dd>
                        </div>
                        <div class="row" ng-if="grbioPersonKey.person.mailingAddress.postalCode">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.postalCode"></dt>
                            <dd class="col-md-8 col-xs-12">{{grbioPersonKey.person.mailingAddress.postalCode}}</dd>
                        </div>
                        <div class="row" ng-if="grbioPersonKey.person.mailingAddress.city">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.city"></dt>
                            <dd class="col-md-8 col-xs-12">{{grbioPersonKey.person.mailingAddress.city}}</dd>
                        </div>
                        <div class="row" ng-if="grbioPersonKey.person.mailingAddress.country">
                            <dt class="col-md-4 col-xs-12" translate="grbio.contact.country"></dt>
                            <dd class="col-md-8 col-xs-12" translate="country.{{grbioPersonKey.person.mailingAddress.country}}"></dd>
                        </div>
                    </dl>
                </div>
            </div>
        </div>
    </div>
</section>