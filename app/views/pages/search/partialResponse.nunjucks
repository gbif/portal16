{% if results.isPartialResponse %}

<div class="horizontal-stripe light-background">
    <div class="container--narrow">
        {$ __('search.partialResultExplanation') $}
    </div>
</div>

{% endif %}