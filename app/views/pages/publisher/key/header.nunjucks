<div class="horizontal-stripe article-header white-background p-b-1">
    <div class="container">
        <div class="row">
            <header class="col-xs-12">
                <nav class="article-header__category article-header__category--deep">
                    <span class="article-header__category__upper"><a
                                ui-sref="publisherSearchList()" translate="stdTerms.publisher">
                        </a></span>
                    <span class="article-header__category__lower" translate="stdTerms.sinceDate" translate-values="{DATE: (publisherKey.publisher.created | momentFormat:'D MMMM YYYY') }">
                        <!--Since DATE-->
                    </span>
                </nav>
                <h1>
                    <span ng-class="{'text-error': publisherKey.publisher.deleted}">{{ publisherKey.publisher.title }}</span>
                </h1>
                <p class="text-error text-center" ng-if="publisherKey.publisher.deleted" translate="phrases.deletedOnDate" translate-values="{DATE: (publisherKey.publisher.deleted | momentFormat:'D MMMM YYYY') }">
                    <!--This publisher was deleted on DATE-->
                </p>
            </header>
        </div>

        {#<div class="stripe-comment" ng-if="publisherKey.publisher.modified !== publisherKey.publisher.created">#}
            {#<div>#}
                {#Modified {{ publisherKey.publisher.modified | momentFormat:'D MMMM YYYY' }}#}
            {#</div>#}
        {#</div>#}
    </div>
</div>
